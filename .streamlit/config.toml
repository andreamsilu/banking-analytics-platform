[server]
headless = true
enableCORS = false
enableXsrfProtection = true

[browser]
gatherUsageStats = false

# Theme is intentionally NOT locked here.
# Users can switch Light / Dark from the app menu (⋮ → Settings → Theme).
# Only brand accent is suggested via custom CSS that adapts to both themes.
